<%- model_class = EvolucaoTipo -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:nome) %>:</strong></dt>
  <dd><%= @evolucao_tipo.nome %></dd>
</dl>

<% @evolucao_tipo.campos.order(:nome).each do |c| %>
  <dl class="dl-horizontal">
    <dt><strong><%= c.nome %></strong></dt>
    <dd><%= (c.tipo_campo == 'check_box' ? '[]' : '--') + ' ' + c.titulo + ' ' + (c.requerido ? '(Requerido)' : '') %></dd>
  </dl>
<% end %>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              evolucao_tipos_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_evolucao_tipo_path(@evolucao_tipo), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              evolucao_tipo_path(@evolucao_tipo),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
